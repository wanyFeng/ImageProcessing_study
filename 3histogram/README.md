# 图像直方图

本目录用于学习灰度直方图、直方图均衡化和直方图匹配。

## 目录内容

- `histogram.md`：理论、公式和实验结果说明。
- `red.jpg`、`yellow.jpg`：原始彩色图片。
- `code/`：灰度输入、实现代码和输出结果。

## 使用方法

1. 阅读 `histogram.md` 理解直方图及累计分布函数。
2. 进入 `code/`，按照其中的 `README.md` 依次运行三个任务。

运行代码前需要安装：

```powershell
python -m pip install numpy opencv-python
```

